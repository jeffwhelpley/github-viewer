/**
 * main.config
 *
 * For now only one config, but we could switch it up in the future
 */
define(function() {
    return {

        // no config files so far, but placeholder for future

    };
});


